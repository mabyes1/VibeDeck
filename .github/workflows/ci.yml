name: CI

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

jobs:
  verify:
    runs-on: windows-latest
    timeout-minutes: 20
    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Set up .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Run canonical source gate
        shell: pwsh
        run: .\scripts\test-product-flow.ps1 -Source

      - name: Audit Windows release dependency graph
        shell: pwsh
        run: dotnet restore .\src\VibeDeck.Host\VibeDeck.Host.csproj -r win-x64 --force
